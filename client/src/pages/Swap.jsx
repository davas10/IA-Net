
import { useStateContext } from '../context/index';


const Swap = () => {
  const { currentColor, currentMode } = useStateContext();

  return (
    <div className="mt-24">
     
    </div>
  );
};

export default Swap;